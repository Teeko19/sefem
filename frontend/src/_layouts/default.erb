<!doctype html>
<html lang="<%= site.locale %>">
  <head>
    <%= render "head", metadata: site.metadata, title: resource.data.title %>
  </head>
  <body class="bg-z0 text-fg <%= resource.data.layout %> <%= resource.data.page_class %>">
    <%= render "header" %>

    <main>
      <%= yield %>
    </main>

    <%# render "footer", metadata: site.metadata %>
  </body>
</html>
